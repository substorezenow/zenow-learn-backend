#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast, reliable local checks for commits
# 1) TypeScript type-check (no emit)
npx tsc -p tsconfig.json --noEmit || exit 1

# 2) Skip unit/integration tests locally; run them in CI instead
echo "Skipping tests in pre-commit; CI runs full suite"
